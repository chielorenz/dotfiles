# Alacritty custom configuration
# see https://github.com/alacritty/alacritty/blob/master/alacritty.yml

import:
  - ~/.config/alacritty/github-dimmed.yml # must be absolute path

window:
  padding:
    x: 10
    y: 10

font:
  size: 16
  normal:
    family: FiraMono Nerd Font Mono
